#!/usr/bin/env bash
# Configure Nginx to listen on port 80 and reload the service

# Update the listen port from 8080 to 80
sed -i 's/listen 8080;/listen 80;/g' /etc/nginx/sites-enabled/default

# Use 'nginx -s reload' which is often more reliable than 'service restart' in containers
nginx -s reload
