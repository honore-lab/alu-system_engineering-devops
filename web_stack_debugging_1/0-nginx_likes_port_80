#!/usr/bin/env bash
# This script configures Nginx to listen on port 80 and ensures the service is running.

# Use a more flexible sed command to change any 'listen' directive to port 80
sed -i 's/listen [0-9]\+/listen 80/g' /etc/nginx/sites-enabled/default

# Restart the service to apply changes
service nginx restart
